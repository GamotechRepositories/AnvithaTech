# Workspace Rules & Guidelines

## Git Workflow
- **No Automatic Push**: Never run `git push` automatically.
- **Explicit Instruction Only**: Only push commits to remote repositories (`origin`, `personal`, etc.) when the user explicitly instructs to do so (e.g., "push kar", "push changes", "push to github").
